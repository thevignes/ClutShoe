<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - ClutchShoe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #1DB954;
            --text-color: #FFFFFF;
            --background-start: #121212;
            --background-end: #191414;
            --error-color: #ff4444;
            --input-background: #282828;
            --input-text: #FFFFFF;
            --input-border: #404040;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Circular', Helvetica, Arial, sans-serif;
            background: linear-gradient(to bottom right, var(--background-start), var(--background-end));
            color: var(--text-color);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .register-container {
            display: flex;
            width: 750px;
            max-width: 90%;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 8px;
            overflow: hidden;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
            margin: 20px auto;
            height: 600px;
        }

        .brand-section {
            flex: 0 0 45%;
            padding: 30px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background: linear-gradient(45deg, rgba(29, 185, 84, 0.1), transparent);
            position: relative;
            overflow: hidden;
        }

        .brand-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('https://i.pinimg.com/474x/8f/38/5e/8f385ed1e6d4828572ebf04d9f9bee50.jpg') center/contain no-repeat;
            opacity: 0.8;
            z-index: 0;
            transform: rotate(-15deg) scale(1.2);
            filter: drop-shadow(0 0 20px rgba(0, 0, 0, 0.3));
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% {
                transform: rotate(-15deg) scale(1.2) translateY(0);
            }
            50% {
                transform: rotate(-15deg) scale(1.2) translateY(-20px);
            }
        }

        .brand-content {
            position: relative;
            z-index: 1;
            text-align: center;
            background: rgba(0, 0, 0, 0.6);
            padding: 30px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
        }

        .website-name {
            font-size: 3.5rem;
            font-weight: bold;
            margin-bottom: 20px;
            color: var(--text-color);
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .brand-tagline {
            font-size: 1.2rem;
            color: var(--text-color);
            line-height: 1.6;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }

        .form-section {
            flex: 0 0 55%;
            padding: 30px;
            background: rgba(0, 0, 0, 0.3);
        }

        .register-card {
            max-width: 350px;
            margin: 0 auto;
        }

        .register-title {
            font-size: 1.8rem;
            font-weight: bold;
            margin-bottom: 25px;
            text-align: center;
            color: var(--text-color);
        }

        .form-control {
            background-color: var(--input-background);
            border: 1px solid var(--input-border);
            color: var(--input-text);
            padding: 10px 12px;
            border-radius: 4px;
            margin-bottom: 15px;
            font-size: 0.9rem;
            height: 40px;
        }

        .form-control:focus {
            background-color: var(--input-background);
            border-color: var(--primary-color);
            color: var(--input-text);
            box-shadow: 0 0 0 2px rgba(29, 185, 84, 0.2);
        }

        .form-label {
            font-size: 0.85rem;
            margin-bottom: 6px;
            color: var(--text-color);
        }

        .btn-primary {
            background-color: var(--primary-color);
            border: none;
            padding: 10px;
            font-weight: 700;
            font-size: 0.9rem;
            border-radius: 25px;
            height: 40px;
            width: 100%;
            margin-top: 10px;
        }

        .btn-primary:hover {
            background-color: #1ed760;
            transform: scale(1.02);
        }

        .divider {
            text-align: center;
            margin: 15px 0;
            position: relative;
        }

        .divider::before,
        .divider::after {
            content: '';
            position: absolute;
            top: 50%;
            width: 42%;
            height: 1px;
            background-color: rgba(255, 255, 255, 0.1);
        }

        .divider::before {
            left: 0;
        }

        .divider::after {
            right: 0;
        }

        .divider span {
            background: transparent;
            padding: 0 10px;
            color: #a7a7a7;
            font-size: 0.85rem;
        }

        .btn-google {
            background-color: transparent;
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: var(--text-color);
            padding: 8px;
            font-size: 0.9rem;
            border-radius: 25px;
            width: 100%;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            transition: all 0.2s;
        }

        .btn-google:hover {
            border-color: var(--text-color);
            transform: scale(1.02);
        }

        .btn-google i {
            font-size: 1.1rem;
        }

        .text-danger {
            color: var(--error-color) !important;
            font-size: 0.85rem;
            margin-top: 5px;
        }

        @media (max-width: 768px) {
            .register-container {
                flex-direction: column;
                width: 95%;
            }

            .brand-section,
            .form-section {
                padding: 20px;
            }

            .website-name {
                font-size: 2.5rem;
            }

            .brand-tagline {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="brand-section">
            <!-- <div class="brand-content">
                <h1 class="website-name">ClutchShoe</h1>
                <p class="brand-tagline">Step into style with our premium collection of footwear. Your perfect pair awaits.</p>
            </div> -->
        </div>

        <div class="form-section">
            <div class="register-card">
                <h3 class="register-title">Create Account</h3>
                <form id="signupForm" action="/register" method="POST" onsubmit="return validateForm()">
                    <div id="allFieldsError" class="text-danger text-center mb-3"></div>

                    <div class="mb-3">
                        <label for="name" class="form-label">Full Name</label>
                        <input type="text" class="form-control" id="name" name="name" placeholder="Enter your full name">
                        <div id="nameError" class="text-danger"></div>
                    </div>

                    <div class="mb-3">
                        <label for="email" class="form-label">Email Address</label>
                        <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email address">
                        <div id="emailError" class="text-danger"></div>
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" placeholder="Choose a strong password">
                        <div id="passwordError" class="text-danger"></div>
                    </div>

                    <div class="mb-4">
                        <label for="confirmPassword" class="form-label">Confirm Password</label>
                        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" placeholder="Confirm your password">
                        <div id="confirmPasswordError" class="text-danger"></div>
                    </div>

                    <div class="d-grid mb-3">
                        <button type="submit" class="btn btn-primary">
                            <span>Create Account</span>
                        </button>
                    </div>

                    <div class="divider">
                        <span>or continue with</span>
                    </div>

                    <div class="d-grid">
                        <button type="button" class="btn btn-google" onclick="window.location.href='/auth/google'">
                            <i class="fa-brands fa-google"></i>
                            <span>Google</span>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script src="/evara-frontend/assets/js/userValidate.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        
    </script>
</body>
</html>
